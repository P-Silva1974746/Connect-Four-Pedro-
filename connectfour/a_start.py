def heuristic(board, player):
    for row in range (len(board)-1,0,-1): #search 
        for col in (0,len(board),1):
            if(board[row][col]==' '):




def a_star (board,player):
    row=-1


    return row